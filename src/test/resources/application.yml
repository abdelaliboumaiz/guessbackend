server:
  port: ${SERVER_PORT:8080}
spring:
  jpa:
    hibernate:
      ddl-auto: update
    database : mysql
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/guessDatabase}
    username: root
    password: root 
    plateforme : mysql